[project]
name = "crm-sales-analytics-platform"
version = "0.1.0"
description = "End-to-end CRM sales analytics pipeline with Snowflake, dbt, and Power BI"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dbt-snowflake>=1.11.4",
    "numpy>=2.4.4",
    "pandas>=3.0.2",
]
